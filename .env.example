TS_AUTHKEY=
API_KEY=
NODE_HOST=0.0.0.0
SERVICE_PORT=62050
API_PORT=62051
SERVICE_PROTOCOL=grpc
DEBUG=false

# =============================================================================
# PasarGuard Node – SSL/TLS
# در Docker مسیرها داخل کانتینر هستند (در docker-compose ست شده).
# برای اجرای غیر Docker این‌ها را تنظیم کن.
# =============================================================================
# SSL_CERT_FILE=/app/certs/ssl_cert.pem
# SSL_KEY_FILE=/app/certs/ssl_key.pem

# =============================================================================
# PasarGuard Node – Xray (پیش‌فرض‌ها معمولاً کافی‌اند)
# هسته: فقط xray-core پشتیبانی می‌شود.
# =============================================================================
# XRAY_EXECUTABLE_PATH=/usr/local/bin/xray
# XRAY_ASSETS_PATH=/usr/local/share/xray
